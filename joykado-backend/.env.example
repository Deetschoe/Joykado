# Port for the server to listen on
PORT=3000

# Environment (development or production)
NODE_ENV=production

# Database configuration (if using PostgreSQL instead of SQLite)
# DATABASE_URL=postgresql://user:password@localhost:5432/joykado

# Optional: File upload size limit in bytes (default: 50MB)
# MAX_FILE_SIZE=52428800

# Optional: Allowed file types
# ALLOWED_AUDIO_TYPES=audio/mpeg,audio/mp3,audio/wav
